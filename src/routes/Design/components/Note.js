import React, { Component } from 'react'
import './Note.scss'

export default class Note extends Component {
  render () {
    return (
      <div id='messageBox' className='design_note' />
    )
  }
}
